import { Text, View } from "react-native"

const BracketPage = ({navigation, route}) => {
    return (
        <View>
            <Text>Test</Text>
        </View>
    )
}

export default BracketPage;
