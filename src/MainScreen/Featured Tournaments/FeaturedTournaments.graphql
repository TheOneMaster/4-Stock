query FeaturedTournaments {
  tournaments(
    query: { perPage: 8, filter: { staffPicks: true, past: false } }
  ) {
    nodes {
      id
      name
      images(type: "profile") {
        id
        type
        url
      }
    }
  }
}
